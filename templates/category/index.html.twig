{% extends 'base.html.twig' %}

{% block title %}Categories{% endblock %}

{% block body %}
    {% for category in categories %}
    <div class="list-group">
        <a href="/wild/category/{{ categoryName }}" class="list-group-item list-group-item-action active">{{ category.name }}</a>
    {% endfor %}
{% endblock %}