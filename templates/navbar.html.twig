<nav class="navbar navbar-expand-lg navbar-light">
    <a class="navbar-brand" href="{{ path('app_index') }}">Wild Series</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('wild_index') }}">Programs<span class="sr-only">(current)</span></a>
            </li>
            {% if is_granted('ROLE_ADMIN')  %}
                <li class="nav-item">
                    <a class="nav-link" href="/program">Admin</a>
                </li>
            {% endif %}
        </ul>
        {% if app.user %}
            <div class="mx-4"><a href="{{ path('profile') }}" class="text-dark">{{ app.user.firstname }}</a></div>
            <a href="{{ path('app_logout') }}" class="text-dark"> Sign Out</a>
        {% else %}
            <a href="{{ path('app_login') }}" class="text-dark"> Sign In</a>
        {% endif %}
    </div>
</nav>
